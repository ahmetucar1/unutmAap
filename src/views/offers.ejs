<h1>Bize Bir Öneride Bulun</h1>



<div class="row">
    <div class="col-lg-5 d-none d-lg-block bg-anything-image"></div>
    <div class="col-lg-7">
        <div class="p-5">
            <div class="text-center">
                <p>Uygulamamızı kullandıysan formatımızı anlamışsındır. "Keşke falanca şeyi de kaydetseydi." dediğin bir önerin varsa bunu bize bildir.Sana muhakkak dönüş yapacağız.</p>
                <% if (locals.validation_error != undefined) { %>
                    <%- include('./partials/_mesaj') %> 
               <%  } %> 
            </div>
            <form class="öneri" action="/administration/offers" method="post">
                <div class="form-group row">
                    <div class="col-sm-6 mb-3 mb-sm-0">
                        <input type="email" value="<%= typeof email != 'undefined' ? email : ''%>" name="email" 
                        class="form-control form-control-user" id="exampleLastName"
                            placeholder="Emailiniz">
                    </div>
                    <div class="col-sm-6">
                        <textarea class="form-control form-control-user" name="offer" rows="10" cols="30" wrap="soft" 
                        value="<%= typeof offer != 'undefined' ? offer : ''%>" > Önerini yaz. </textarea>
                    </div>
                </div>
                <!--
                <div class="form-group row">
                    <div class="col-sm-6 mb-3 mb-sm-0">
                        <input type="datetime-local" value=" name="date" 
                        class="form-control form-control-user"
                            id="exampleInputPassword">
                            <p>Ne zaman izleyeceksin? Belli değilse boş bırakabilirsin.</p>
                    </div>
                   
            <div class="col-sm-6">
                <input type="text" value="<%= typeof date != 'undefined' ? date : ''%>" name= "date" 
                    class="form-control form-control-user"
                    id="exampleLastName" placeholder="Tarih(İsteğe Bağlı)">
            </div>
                </div>
                 -->
                <button type="submit" class="btn btn-primary btn-user btn-block">
                    Gönder
                </button>
                