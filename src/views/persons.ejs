<h1>Kişilerini Kaydet</h1>



<div class="row">
    <div class="col-lg-5 d-none d-lg-block bg-person-image"></div>
    <div class="col-lg-7">
        <div class="p-5">
            <div class="text-center">
                <h1 class="h4 text-gray-900 mb-4">Kişi Kaydedin</h1>
                <% if (locals.validation_error != undefined) { %>
                    <%- include('./partials/_mesaj') %> 
               <%  } %> 
            </div>
            <form class="kitap" action="/administration/persons" method="post">
                <div class="form-group row">
                    <div class="col-sm-6 mb-3 mb-sm-0">
                        <input type="text" value="<%= typeof personName != 'undefined' ? personName : ''%>" name="personName" 
                        class="form-control form-control-user" id="exampleLastName"
                            placeholder="Kişi adı">
                    </div>
                    <div class="col-sm-6">
                        <input type="tel" value="<%= typeof phone != 'undefined' ? phone : ''%>" name="phone" 
                        class="form-control form-control-user" id="exampleLastName"
                            placeholder="Telefon Numarası">
                    </div>
                </div>
                <!--
                <div class="form-group row">
                    <div class="col-sm-6 mb-3 mb-sm-0">
                        <input type="datetime-local" value=" name="date" 
                        class="form-control form-control-user"
                            id="exampleInputPassword">
                            <p>Ne zaman izleyeceksin? Belli değilse boş bırakabilirsin.</p>
                    </div>
                    -->
            <div class="col-sm-6">
                <input type="text" value="<%= typeof other!= 'undefined' ? other : ''%>" name= "other" 
                    class="form-control form-control-user"
                    id="exampleLastName" placeholder="Diğer bilgiler(email, adres meslek vb.)">
            </div>
                </div>
                <button type="submit" class="btn btn-primary btn-user btn-block">
                    Kişiyi Kaydet
                </button>